---
import Debug from 'astro/debug';

// interface ProjectMarkdownFrontmatter {
//   title: string;
//   client: string;
//   publishDate: number;
//   img: string;
//   description: string;
//   url: string;
//   tags: Array<string>;
//   astro: {
//     headers: Array<string>;
//     source: string;
//     html: string;
//   }
// };

// const project: ProjectMarkdownFrontmatter = Astro.props
const {project} = Astro.props;
---

<article class="grid border shadow rounded">
  <div class="">
    <img src={project.img} class="h-full rounded-t shadow" />
  </div>
  <div class="bg-white grid  justify-center items-center px-6 py-16 rounded-b shadow-md ">
    <div class="grid gap-y-12">
    <p class="uppercase text-5xl text-gray-900 text-center font-bold">{project.title}</p>
    <p class=" text-gray-900 text-xl text-center" >{project.description}</p>
    <div class="flex justify-around">
      <a class="text-gray-900 text-lg border-2 border-gray-900 uppercase font-bold px-2 py-1 rounded hover:bg-gray-300 hover:scale-110 transition duration-500 ease-in-out transform" href={project.url}>Details</a>
      <a class="text-gray-900 text-lg border-2 border-gray-900 uppercase font-bold px-2 py-1 rounded  hover:bg-gray-300 hover:scale-110 transition duration-500 ease-in-out transform" href={project.link} >Go To Site</a>
    </div>
    </div>
  </div>
</article>
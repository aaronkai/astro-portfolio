---
import InnerBodyWrapper from './InnerBodyWrapper.astro'
import MainHead from '../components/MainHead.astro'
import Debug from 'astro/debug'
const { content } = Astro.props;

---
<html lang="en">
  <head>
    <MainHead />
  </head>
<body>
    <!-- <InnerBodyWrapper>
    <Debug {content} />
      <h1 class="title">{content.title}</h1>
        {content.tags.map((t) => (
          <span class="tag">{t}</span>
        ))}

      <div class="content"><slot></slot></div>
   
    </InnerBodyWrapper> -->
      <InnerBodyWrapper>
      <main class="grid max-w-xl gap-4 px-6 mx-auto mt-8 md:gap-12 " >
        <img src={content.img} />
        <header class="grid gap-4">
          
          <h1 style="font-size: clamp(2.5rem, 5vw, 5rem);" class="title">{content.title}</h1>

                  <h3 class="tagline">{content.description}</h3>
          <ul type="list" class="flex flex-wrap">
          {content.tags.map((tag) => (
            <span class="px-2 py-1 mb-2 mr-2 font-bold text-center text-white bg-gray-900 rounded">#{tag}</span>
          ))}
          </ul>
        </header>
        <!-- Section below styled in global.css with blog class -->
        <section class="grid gap-4 blog">
          <slot></slot>
        </section>
      </main>
   
    </InnerBodyWrapper>
</body>
</html>